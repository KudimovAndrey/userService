
	upstream  myproject {
		server localhost:3400 max_fails=2 fail_timeout=30s;
		server localhost:3500 backup;
		
	}
	server {
			listen       80;
			server_name  localhost;
			location / {
				 proxy_pass http://myproject;
			}
	}
